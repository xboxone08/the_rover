[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "the_rover"
version = "1.0.0"
dependencies = [
    "gpiozero",
    "pigpio",
    "keyboard",
    "fabric",
]

[project.scripts]
rover = "the_rover:__main__"
